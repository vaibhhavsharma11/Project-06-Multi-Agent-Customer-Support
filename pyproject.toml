[project]
name = "project-06-multi-agent-customer-support"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vaibhav Sharma", email = "vrox002@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "openai>=3.6.0",
    "pydantic>=2.13.5",
    "uvicorn>=0.52.4",
]

[project.scripts]
project-06-multi-agent-customer-support = "project_06_multi_agent_customer_support:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
